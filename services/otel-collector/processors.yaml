processors:
  batch:
    timeout: 2s
  filter/dockerproxy:
    error_mode: ignore
    metrics:
      exclude:
        match_type: expr
        expressions:
          - Label("net.peer.name") == "docker-proxy"
